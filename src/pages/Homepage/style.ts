import styled from "styled-components";

export const HomepageContainer = styled.div`
position: relative;
    .header{
        display: flex;
    }
    .body{
        position: absolute;
        top: 200px;
        left: 50vw;
    }
`;